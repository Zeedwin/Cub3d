#include "Cub3d.h"

t_point    ray_caster(t_runtime    *r)
{
    t_point p = pos_joueur

    X Y joueur = x \ 100 y \ 100
    while (check_case(p))
    {
        if (dir du joueur >= 0 && pi\2) {
            X Y = X+1 Y du joueur
        }
        theta = arctan(element qui fqut X Y, x y joueur)

        if dir joueur > theta {
            tape a droite
        } else {
            tape en haut
        }
    }
}